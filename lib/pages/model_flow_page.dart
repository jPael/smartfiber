import 'package:flutter/material.dart';

class ModelFlowPage extends StatelessWidget {
  const ModelFlowPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Model FLow")],
      ),
    );
  }
}
