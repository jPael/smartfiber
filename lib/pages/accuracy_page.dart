import 'package:flutter/material.dart';

class AccuracyPage extends StatelessWidget {
  const AccuracyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Accuracy")],
      ),
    );
  }
}
